# Database connection string
# Replace user, password, host, port, and database name with your actual PostgreSQL credentials
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Next Auth configuration
# Generate a secret using `openssl rand -base64 32` and paste it here
NEXTAUTH_SECRET=

# Set this to your canonical URL in production
NEXTAUTH_URL=http://localhost:3000

# Node environment (development, production, test)
NODE_ENV=development

# Optional: Set a specific port for the application to run on
# PORT=3000

# Optional: Set a custom log level (error, warn, info, http, verbose, debug, silly)
# LOG_LEVEL=info

# Add any other environment-specific variables below


